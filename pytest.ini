[pytest]
testpaths = tests
markers =
    integration: integration tests that require docker-compose and real Redis
addopts =
    --strict-markers
    --cov=core
    --cov=services
    --cov-report=html
    --cov-report=term-missing
    --cov-report=xml
    --cov-fail-under=70
