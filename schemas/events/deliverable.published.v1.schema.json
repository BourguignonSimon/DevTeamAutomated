{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://audit-flash-os/schemas/events/deliverable.published.v1.schema.json",
  "title": "DELIVERABLE.PUBLISHED payload v1",
  "x_event_type": "DELIVERABLE.PUBLISHED",
  "type": "object",
  "additionalProperties": false,
  "required": ["project_id", "backlog_item_id", "deliverable"],
  "properties": {
    "project_id": {"type": "string", "format": "uuid"},
    "backlog_item_id": {"type": "string", "format": "uuid"},
    "deliverable": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "content", "timestamp", "confidence"],
      "properties": {
        "type": {"type": "string"},
        "content": {"type": "object"},
        "timestamp": {"type": "string", "format": "date-time"},
        "confidence": {"type": "number", "minimum": 0, "maximum": 1},
        "agent": {"type": "string"},
        "project_id": {"type": "string", "format": "uuid"},
        "backlog_item_id": {"type": "string", "format": "uuid"}
      }
    }
  }
}
