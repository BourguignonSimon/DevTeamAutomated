{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://audit-flash-os/schemas/events/human.approval_requested.v1.schema.json",
  "title": "HUMAN.APPROVAL_REQUESTED payload v1",
  "x_event_type": "HUMAN.APPROVAL_REQUESTED",
  "type": "object",
  "required": ["project_id", "backlog_item_id", "reason"],
  "additionalProperties": false,
  "properties": {
    "project_id": {"type": "string", "format": "uuid"},
    "backlog_item_id": {"type": "string"},
    "reason": {"type": "string"},
    "agent": {"type": ["string", "null"]}
  }
}
